---
engines:
  duplication:
    enabled: true
    config:
      languages:
      - ruby
  fixme:
    enabled: true
  rubocop:
    enabled: true
ratings:
  paths:
  - .rubocop.yml
  - "**.rb"
exclude_paths:
- spec/**/*
